// Session cookie configuration, shared by the user creation flow and the
// `authenticate` middleware.
export const SESSION_COOKIE = 'sessionId'
export const SESSION_MAX_AGE_SECONDS = 60 * 60 * 24 * 7 // 7 days
